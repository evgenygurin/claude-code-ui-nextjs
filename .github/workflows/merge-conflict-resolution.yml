name: Robust Merge Conflict Resolution

on:
  push:
    branches: [main, develop, feature/*, hotfix/*]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      resolution_strategy:
        description: 'Strategy for conflict resolution'
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - aggressive
          - conservative
          - manual
      enable_monitoring:
        description: 'Enable continuous monitoring'
        required: false
        default: true
        type: boolean
      notification_level:
        description: 'Notification level'
        required: false
        default: 'normal'
        type: choice
        options:
          - silent
          - normal
          - verbose

permissions:
  contents: read
  issues: write
  pull-requests: read

env:
  NODE_VERSION: '20'
  CONFLICT_RESOLUTION_ENABLED: true

jobs:
  detect-conflicts:
    name: 🔍 Conflict Detection
    runs-on: ubuntu-latest
    outputs:
      has-conflicts: ${{ steps.conflict-check.outputs.has-conflicts }}
      conflict-files: ${{ steps.conflict-check.outputs.conflict-files }}
      conflict-count: ${{ steps.conflict-check.outputs.conflict-count }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name "Merge Conflict Bot"
          git config --global user.email "merge-bot@github-actions.local"

      - name: Detect merge conflicts
        id: conflict-check
        run: |
          echo "🔍 Проверка на наличие конфликтов слияния..."

          # Сначала проверим текущий статус
          git status --porcelain > git_status.txt

          CONFLICT_FILES=""
          CONFLICT_COUNT=0
          HAS_CONFLICTS=false

          # Проверяем файлы с конфликтами в текущем статусе
          if grep -E "^(UU|AA|DD)" git_status.txt > /dev/null 2>&1; then
            HAS_CONFLICTS=true
            CONFLICT_FILES=$(grep -E "^(UU|AA|DD)" git_status.txt | cut -c4- | tr '\n' ',' | sed 's/,$//')
            CONFLICT_COUNT=$(grep -E "^(UU|AA|DD)" git_status.txt | wc -l)
          fi

          # Также проверим маркеры конфликтов в файлах (исключаем файлы, которые законно содержат эти маркеры)
          if git grep -l "<<<<<<< HEAD" -- ':!MERGE_CONFLICT_RESOLUTION.md' ':!scripts/merge-conflict-resolver.js' ':!tests/merge-conflicts/*' ':!.github/workflows/merge-conflict-resolution.yml' > conflict_markers.txt 2>/dev/null; then
            if [ -s conflict_markers.txt ]; then
              HAS_CONFLICTS=true
              MARKER_FILES=$(cat conflict_markers.txt | tr '\n' ',' | sed 's/,$//')
              MARKER_COUNT=$(cat conflict_markers.txt | wc -l)
              
              if [ -n "$CONFLICT_FILES" ]; then
                CONFLICT_FILES="$CONFLICT_FILES,$MARKER_FILES"
              else
                CONFLICT_FILES="$MARKER_FILES"
              fi
              
              CONFLICT_COUNT=$((CONFLICT_COUNT + MARKER_COUNT))
            fi
          fi

          # Попробуем создать тестовое слияние для проверки потенциальных конфликтов
          if [ "$HAS_CONFLICTS" = "false" ] && [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "🔄 Проверка потенциальных конфликтов при слиянии PR..."
            
            # Создаем временную ветку для тестового слияния
            git checkout -b temp-merge-test
            git fetch origin ${{ github.base_ref }}
            
            # Пытаемся выполнить тестовое слияние
            if ! git merge origin/${{ github.base_ref }} --no-commit --no-ff > merge_test.log 2>&1; then
              if git status --porcelain | grep -E "^(UU|AA|DD)" > /dev/null; then
                HAS_CONFLICTS=true
                CONFLICT_FILES=$(git status --porcelain | grep -E "^(UU|AA|DD)" | cut -c4- | tr '\n' ',' | sed 's/,$//')
                CONFLICT_COUNT=$(git status --porcelain | grep -E "^(UU|AA|DD)" | wc -l)
                echo "⚠️ Обнаружены конфликты при тестовом слиянии"
              fi
            fi
            
            # Отменяем тестовое слияние
            git merge --abort 2>/dev/null || true
            git checkout ${{ github.ref_name }}
            git branch -D temp-merge-test
          fi

          echo "has-conflicts=$HAS_CONFLICTS" >> $GITHUB_OUTPUT
          echo "conflict-files=$CONFLICT_FILES" >> $GITHUB_OUTPUT
          echo "conflict-count=$CONFLICT_COUNT" >> $GITHUB_OUTPUT

          if [ "$HAS_CONFLICTS" = "true" ]; then
            echo "⚠️ Обнаружено конфликтов: $CONFLICT_COUNT"
            echo "📁 Файлы с конфликтами: $CONFLICT_FILES"
          else
            echo "✅ Конфликты не обнаружены"
          fi

      - name: Upload conflict detection artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: conflict-detection-${{ github.run_number }}
          path: |
            git_status.txt
            conflict_markers.txt
            merge_test.log
          retention-days: 7

  resolve-conflicts:
    name: 🛠️ Automatic Conflict Resolution
    runs-on: ubuntu-latest
    needs: [detect-conflicts]
    if: needs.detect-conflicts.outputs.has-conflicts == 'true'

    outputs:
      resolution-success: ${{ steps.resolve.outputs.success }}
      resolution-strategy: ${{ steps.resolve.outputs.strategy }}
      resolved-count: ${{ steps.resolve.outputs.resolved-count }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Configure Git
        run: |
          git config --global user.name "Merge Conflict Bot"
          git config --global user.email "merge-bot@github-actions.local"

      - name: Make resolution script executable
        run: chmod +x scripts/merge-conflict-resolver.js

      - name: Automatic conflict resolution
        id: resolve
        run: |
          echo "🛠️ Запуск автоматического разрешения конфликтов..."

          STRATEGY="${{ github.event.inputs.resolution_strategy || 'auto' }}"
          echo "📋 Используемая стратегия: $STRATEGY"

          # Создаем отчетную директорию
          mkdir -p .conflict-resolution-reports

          # Запускаем резолвер с подробным логированием
          if node scripts/merge-conflict-resolver.js --verbose > resolution.log 2>&1; then
            echo "✅ Конфликты успешно разрешены автоматически"
            echo "success=true" >> $GITHUB_OUTPUT
            echo "strategy=$STRATEGY" >> $GITHUB_OUTPUT
            
            # Подсчитываем количество разрешенных файлов
            RESOLVED_COUNT=$(git diff --cached --name-only | wc -l)
            echo "resolved-count=$RESOLVED_COUNT" >> $GITHUB_OUTPUT
            
            # Сохраняем статистику
            echo "📊 Разрешено файлов: $RESOLVED_COUNT"
            
          else
            echo "❌ Не удалось разрешить все конфликты автоматически"
            echo "success=false" >> $GITHUB_OUTPUT
            echo "strategy=failed" >> $GITHUB_OUTPUT
            echo "resolved-count=0" >> $GITHUB_OUTPUT
          fi

          # Показываем лог
          echo "📝 Лог разрешения конфликтов:"
          cat resolution.log || echo "Лог недоступен"

          # Показываем текущий статус
          echo "📊 Текущий статус git:"
          git status --porcelain

      - name: Validate resolution
        if: steps.resolve.outputs.success == 'true'
        run: |
          echo "🔍 Проверка качества разрешения..."

          # Проверяем, не остались ли маркеры конфликтов (исключаем файлы, которые законно содержат эти маркеры)
          if git grep -l "<<<<<<< HEAD" -- ':!MERGE_CONFLICT_RESOLUTION.md' ':!scripts/merge-conflict-resolver.js' ':!tests/merge-conflicts/*' ':!.github/workflows/merge-conflict-resolution.yml' > remaining_conflicts.txt 2>/dev/null; then
            if [ -s remaining_conflicts.txt ]; then
              echo "⚠️ Найдены оставшиеся маркеры конфликтов в неожиданных файлах:"
              cat remaining_conflicts.txt
              exit 1
            fi
          fi

          # Проверяем синтаксис основных файлов
          echo "🔍 Проверка синтаксиса..."

          # Проверка JavaScript/TypeScript файлов
          for file in $(git diff --cached --name-only | grep -E '\.(js|ts|jsx|tsx)$'); do
            if [ -f "$file" ]; then
              echo "Проверка синтаксиса: $file"
              if ! node -c "$file" 2>/dev/null; then
                echo "❌ Синтаксическая ошибка в $file"
                exit 1
              fi
            fi
          done

          # Проверка JSON файлов
          for file in $(git diff --cached --name-only | grep '\.json$'); do
            if [ -f "$file" ]; then
              echo "Проверка JSON: $file"
              if ! jq . "$file" >/dev/null 2>&1; then
                echo "❌ Ошибка в JSON файле $file"
                exit 1
              fi
            fi
          done

          echo "✅ Валидация прошла успешно"

      - name: Create resolution commit
        if: steps.resolve.outputs.success == 'true' && github.event_name != 'pull_request'
        run: |
          echo "💾 Создание коммита с разрешенными конфликтами..."

          # Добавляем все разрешенные файлы
          git add -A

          # Создаем коммит
          git commit -m "🔧 Автоматическое разрешение конфликтов слияния

          Стратегия: ${{ steps.resolve.outputs.strategy }}
          Разрешено файлов: ${{ steps.resolve.outputs.resolved-count }}
          Workflow: ${{ github.workflow }} (#${{ github.run_number }})

          🤖 Генерировано автоматически системой разрешения конфликтов" || echo "Нечего коммитить"

      - name: Push resolved changes
        if: steps.resolve.outputs.success == 'true' && github.event_name != 'pull_request'
        run: |
          echo "🚀 Отправка разрешенных изменений..."
          git push origin ${{ github.ref_name }} || echo "Не удалось отправить изменения"

      - name: Upload resolution artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: conflict-resolution-${{ github.run_number }}
          path: |
            resolution.log
            merge-resolution-report-*.json
            .conflict-resolution-reports/
            .merge-backups/
          retention-days: 30

  monitoring-setup:
    name: 📡 Setup Continuous Monitoring
    runs-on: ubuntu-latest
    needs: [detect-conflicts, resolve-conflicts]
    if: always() && (needs.detect-conflicts.outputs.has-conflicts == 'true' || github.event.inputs.enable_monitoring == 'true')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Configure monitoring
        run: |
          echo "📡 Настройка системы мониторинга..."

          MONITORING_LEVEL="${{ github.event.inputs.notification_level || 'normal' }}"
          RESOLUTION_SUCCESS="${{ needs.resolve-conflicts.outputs.resolution-success }}"

          echo "📋 Уровень мониторинга: $MONITORING_LEVEL"

          # Настраиваем интервалы мониторинга в зависимости от ситуации
          if [ "$RESOLUTION_SUCCESS" = "false" ]; then
            # Если разрешение не удалось, включаем интенсивный мониторинг
            MONITORING_INTERVAL=10000  # 10 секунд
          elif [ "${{ needs.detect-conflicts.outputs.has-conflicts }}" = "true" ]; then
            # Если были конфликты, включаем регулярный мониторинг
            MONITORING_INTERVAL=30000  # 30 секунд
          else
            # Стандартный мониторинг
            MONITORING_INTERVAL=60000  # 1 минута
          fi

          echo "⏱️ Интервал мониторинга: ${MONITORING_INTERVAL}ms"

      - name: Test monitoring system
        run: |
          echo "🧪 Тестирование системы мониторинга..."

          # Делаем скрипт исполняемым
          chmod +x scripts/merge-monitor.js

          # Запускаем краткий тест системы
          timeout 30s node scripts/merge-monitor.js --interval=5000 --log-level=debug || echo "Тест мониторинга завершен"

      - name: Schedule monitoring tasks
        run: |
          echo "📅 Планирование задач мониторинга..."

          # В реальной среде здесь можно настроить cron-задачи или другие планировщики
          # Для GitHub Actions создаем файл с настройками для последующего использования

          cat > monitoring-config.json << EOF
          {
            "enabled": true,
            "interval": ${MONITORING_INTERVAL:-30000},
            "notificationLevel": "${{ github.event.inputs.notification_level || 'normal' }}",
            "repositoryUrl": "${{ github.repository }}",
            "workflow": "${{ github.workflow }}",
            "runId": "${{ github.run_id }}",
            "triggeredBy": "${{ github.event_name }}",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          }
          EOF

          echo "✅ Конфигурация мониторинга сохранена"

      - name: Upload monitoring configuration
        uses: actions/upload-artifact@v4
        with:
          name: monitoring-config-${{ github.run_number }}
          path: |
            monitoring-config.json
          retention-days: 7

  notification-summary:
    name: 📢 Resolution Summary & Notifications
    runs-on: ubuntu-latest
    needs: [detect-conflicts, resolve-conflicts, monitoring-setup]
    if: always()

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate summary report
        run: |
          echo "📊 Генерация итогового отчета..."

          HAS_CONFLICTS="${{ needs.detect-conflicts.outputs.has-conflicts }}"
          CONFLICT_COUNT="${{ needs.detect-conflicts.outputs.conflict-count }}"
          CONFLICT_FILES="${{ needs.detect-conflicts.outputs.conflict-files }}"
          RESOLUTION_SUCCESS="${{ needs.resolve-conflicts.outputs.resolution-success }}"
          RESOLVED_COUNT="${{ needs.resolve-conflicts.outputs.resolved-count }}"
          STRATEGY="${{ needs.resolve-conflicts.outputs.resolution-strategy }}"

          echo "## 🔧 Отчет о разрешении конфликтов слияния" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Репозиторий:** ${{ github.repository }}" >> $GITHUB_STEP_SUMMARY
          echo "**Ветка:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Workflow:** ${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "**Событие:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### 🔍 Результаты детекции" >> $GITHUB_STEP_SUMMARY
          if [ "$HAS_CONFLICTS" = "true" ]; then
            echo "- **Статус:** ⚠️ Конфликты обнаружены" >> $GITHUB_STEP_SUMMARY
            echo "- **Количество:** $CONFLICT_COUNT файлов" >> $GITHUB_STEP_SUMMARY
            echo "- **Файлы:** \`$CONFLICT_FILES\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Статус:** ✅ Конфликты не обнаружены" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$HAS_CONFLICTS" = "true" ]; then
            echo "### 🛠️ Результаты разрешения" >> $GITHUB_STEP_SUMMARY
            if [ "$RESOLUTION_SUCCESS" = "true" ]; then
              echo "- **Статус:** ✅ Успешно разрешено автоматически" >> $GITHUB_STEP_SUMMARY
              echo "- **Стратегия:** $STRATEGY" >> $GITHUB_STEP_SUMMARY
              echo "- **Разрешено файлов:** $RESOLVED_COUNT" >> $GITHUB_STEP_SUMMARY
            else
              echo "- **Статус:** ❌ Требуется ручное вмешательство" >> $GITHUB_STEP_SUMMARY
              echo "- **Причина:** Автоматическое разрешение не удалось" >> $GITHUB_STEP_SUMMARY
            fi
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "### 📡 Мониторинг" >> $GITHUB_STEP_SUMMARY
          echo "- **Статус:** ${{ needs.monitoring-setup.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Уведомления:** ${{ github.event.inputs.notification_level || 'normal' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### 📋 Следующие шаги" >> $GITHUB_STEP_SUMMARY
          if [ "$HAS_CONFLICTS" = "false" ]; then
            echo "- ✅ Никаких дополнительных действий не требуется" >> $GITHUB_STEP_SUMMARY
          elif [ "$RESOLUTION_SUCCESS" = "true" ]; then
            echo "- ✅ Конфликты разрешены, можно продолжить работу" >> $GITHUB_STEP_SUMMARY
            echo "- 📡 Мониторинг настроен для отслеживания дальнейших изменений" >> $GITHUB_STEP_SUMMARY
          else
            echo "- 🚨 **Требуется ручное вмешательство разработчика**" >> $GITHUB_STEP_SUMMARY
            echo "- 🔍 Проверьте файлы с конфликтами: \`$CONFLICT_FILES\`" >> $GITHUB_STEP_SUMMARY
            echo "- 🛠️ Разрешите конфликты вручную и выполните commit" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Create GitHub issue for manual intervention
        if: needs.detect-conflicts.outputs.has-conflicts == 'true' && needs.resolve-conflicts.outputs.resolution-success != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const conflictFiles = '${{ needs.detect-conflicts.outputs.conflict-files }}';
            const conflictCount = '${{ needs.detect-conflicts.outputs.conflict-count }}';
            const runUrl = '${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}';

            const issueBody = [
              '🚨 Требуется ручное разрешение конфликтов слияния',
              '',
              'Детали конфликтов:',
              '- Количество файлов: ' + conflictCount,
              '- Затронутые файлы: ' + conflictFiles,
              '- Ветка: ${{ github.ref_name }}',
              '- Workflow Run: ' + runUrl,
              '- Событие: ${{ github.event_name }}',
              '- Временная метка: ' + new Date().toISOString(),
              '',
              '---',
              '',
              '## 🛠️ Инструкции по разрешению:',
              '',
              '1. Получите изменения:',
              '   ```bash',
              '   git checkout ${{ github.ref_name }}',
              '   git pull origin ${{ github.ref_name }}',
              '   ```',
              '',
              '2. Разрешите конфликты:',
              '   - Откройте каждый файл с конфликтами в вашем редакторе',
              '   - Найдите маркеры конфликтов: <<<<<<< HEAD, =======, >>>>>>> branch',
              '   - Выберите правильную версию кода или объедините изменения',
              '   - Удалите маркеры конфликтов',
              '',
              '3. Подготовьте изменения:',
              '   ```bash',
              '   git add <разрешенные_файлы>',
              '   git commit -m "resolve: исправление конфликтов слияния"',
              '   ```',
              '',
              '4. Отправьте изменения:',
              '   ```bash',
              '   git push origin ${{ github.ref_name }}',
              '   ```',
              '',
              '## 🤖 Автоматические попытки разрешения:',
              '',
              'Система автоматического разрешения конфликтов попыталась обработать эти файлы, но потребовалось ручное вмешательство. Возможные причины:',
              '- Сложные конфликты кода, требующие человеческого анализа',
              '- Конфликты в критически важных файлах',
              '- Множественные перекрывающиеся изменения',
              '',
              '## 📞 Помощь:',
              '',
              'Если у вас возникли трудности, обратитесь к документации по разрешению конфликтов или к команде разработки.',
              '',
              '---',
              '',
              'Это автоматическое issue создано системой разрешения конфликтов слияния.'
            ].join('\n');

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `🚨 Ручное разрешение конфликтов: ${conflictCount} файлов - ${{ github.ref_name }}`,
              body: issueBody,
              labels: ['merge-conflicts', 'manual-intervention', 'high-priority', 'automated']
            });

      - name: Comment on PR
        if: github.event_name == 'pull_request' && needs.detect-conflicts.outputs.has-conflicts == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const conflictFiles = '${{ needs.detect-conflicts.outputs.conflict-files }}';
            const conflictCount = '${{ needs.detect-conflicts.outputs.conflict-count }}';
            const resolutionSuccess = '${{ needs.resolve-conflicts.outputs.resolution-success }}';
            const resolvedCount = '${{ needs.resolve-conflicts.outputs.resolved-count }}';

            let commentBody = `## 🔧 Результаты обработки конфликтов слияния\n\n`;

            if (resolutionSuccess === 'true') {
              commentBody += `✅ Конфликты успешно разрешены автоматически!\n\n`;
              commentBody += `- 📊 Обработано файлов: ${resolvedCount}\n`;
              commentBody += `- 🛠️ Стратегия: ${{ needs.resolve-conflicts.outputs.resolution-strategy }}\n`;
              commentBody += `- 🤖 Изменения были применены автоматически\n\n`;
              commentBody += `Вы можете продолжить review этого PR.`;
            } else {
              commentBody += `⚠️ Обнаружены конфликты, требующие ручного разрешения\n\n`;
              commentBody += `- 📊 Файлов с конфликтами: ${conflictCount}\n`;
              commentBody += `- 📁 Затронутые файлы: \`${conflictFiles}\`\n\n`;
              commentBody += `### 🛠️ Что нужно сделать:\n`;
              commentBody += `1. Получите последние изменения из ветки\n`;
              commentBody += `2. Разрешите конфликты в указанных файлах\n`;
              commentBody += `3. Сделайте commit с разрешенными конфликтами\n`;
              commentBody += `4. Push изменений запустит повторную проверку\n\n`;
            }

            commentBody += `---\n*🤖 Автоматический комментарий от системы разрешения конфликтов*`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: commentBody
            });
